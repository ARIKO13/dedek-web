{
  "name": "dedek-tersayang-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Dedek Tersayang bot - LLM chat + reminders + notifications",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
